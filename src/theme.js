// src/theme.js
import { createTheme } from '@mui/material/styles';

// Crea un tema personalizado
const customFontTheme2 = createTheme({
    typography: {
        fontFamily: '"MiFuentePersonalizada", "Roboto", "Helvetica", "Arial", sans-serif',
    },
});

export default customFontTheme2;
