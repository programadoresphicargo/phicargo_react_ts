import BaseLayout from '@/layouts/BaseLayout';
import { ReactNode } from 'react';

export const DriverBonusLayout = ({ children }: { children: ReactNode }) => {
  return <BaseLayout pages={[]}>{children}</BaseLayout>;
};

